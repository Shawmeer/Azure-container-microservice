# Gateway port
PORT=8080
WEBSITES_PORT=

# CORS configuration for browser clients
# Use "*" for local/dev, or a comma-separated list for tighter control.
CORS_ORIGIN=*

# Service URLs (use Docker Compose service names, not localhost)
USER_SERVICE_URL=http://user-service:3001
PRODUCT_SERVICE_URL=http://product-service:3002

